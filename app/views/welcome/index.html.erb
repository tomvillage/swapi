<h1>API STARWARS</h1>

<%= render "searches/index" %>


<table>
  <thead>
    <tr>
      <th>Pelicula</th>
      <th>Año</th>
      <th>Numero</th>
      <th>Director</th>
      <th>Productor</th>
    </tr>
  </thead>

  <tbody>
    <% @films.each do |f|%>
      <tr>
        <td><a href="/films/<%=f["url"].split("/")[-1]%>"><%= f["title"] %></a><br> </td>
        <td><%=f["release_date"] %></td>
        <td><%= f[ "episode_id"] %></td>
        <td><%= f["director"] %></td>
        <td><%= f["producer"] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
